<div class="row">
<% @messages.each do |m| %>
	<div class="col-md-4">
		<div class="message">
			<h2 class="message-title"><%= m.title %></h2>
			<%= link_to 'view message', message_path(m), class:"btn-custom" %>
		</div>
	</div>
<% end %>
</div>